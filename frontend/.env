# PUBLIC_* = bukan secret; di-track khusus exception di .gitignore
# URL FastAPI yg dilihat browser (localhost exposed port). Untuk Docker override ke IP/JWT host.
PUBLIC_API_BASE_URL=http://127.0.0.1:8000