# Dependencies
/node_modules
/.pnp
.pnp.*

# Testing
/coverage

# Next.js
/.next/
/out/

# Production
/build

# Misc
.DS_Store
*.pem

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Env (never commit real secrets; .env.example is safe to track)
.env
*.env
.env.local
.env.development
.env.production
!.env.example

# Vercel
.vercel
.vercel/**

# Secrets / credentials fallback patterns
secrets.env
*.secret
credentials.json

# Agent skills & config files (keep core codebase only)
AGENTS.md
CLAUDE.md
.claude/
.agents/
.windsurf/
skills-lock.json
agent-scripts/

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Prisma generated client (built at deploy time)
/src/generated/prisma

# Local run state (pid/log/port) from gcms.sh isolator
/.run/

# Dev/audit helper scripts (not part of core app)
/gcms.sh
/scripts/
scripts-snapshot.mjs
scripts-snapshot.sh
scripts-final.sh
scripts-gcms-shot.mjs
tmp-shot.mjs

# Local asset capture (README/audit screenshots; commit only final curated images)
images_for_ai/
images/raw/

# Editor / OS
.idea/
.vscode/
Thumbs.db

# Local runtime (auto-loop) state
.opencode/

# Design/audit reports (not part of shippable codebase)
design-audit-report.md
anti-slop/

# Backend venv + artefak python
backend/.venv/
backend/__pycache__/
backend/**/__pycache__/
backend/models/*.pkl

# SvelteKit
frontend/.svelte-kit/
frontend/build/
frontend/build_old_root/
frontend/node_modules/

# Docker publik env (PUBLIC_*, bukan secret) — sengaja di-track
!frontend/.env
!frontend/.env.example

# screenshot bukti tidak disimpan lagi ke repo
images/
